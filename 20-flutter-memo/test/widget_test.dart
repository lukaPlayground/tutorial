import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Memo app smoke test', (WidgetTester tester) async {
    // Hive init requires platform channels — skip in unit test
  });
}
