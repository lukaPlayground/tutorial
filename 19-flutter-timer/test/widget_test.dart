import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Timer app smoke test', (WidgetTester tester) async {
    // Smoke test placeholder — notification init requires platform channels
  });
}
